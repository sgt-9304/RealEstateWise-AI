export function redFlags(x:{reraNumber?:string|null;areaType?:string;grossYield?:number;netYield?:number;annualCashFlow?:number;appreciationRate?:number;verified?:boolean;comparableCount?:number}){const f:string[]=[];if(!x.reraNumber)f.push("MahaRERA registration evidence is missing");if(!x.verified)f.push("Official verification has not been completed");if(x.areaType!=="carpet")f.push("Area is not confirmed as carpet area");if((x.appreciationRate||0)>.12)f.push("Appreciation assumption is unusually high and requires justification");if((x.annualCashFlow||0)<0)f.push("Leveraged annual cash flow is negative");if((x.comparableCount||0)<3)f.push("Fewer than three comparable records supplied");return f}