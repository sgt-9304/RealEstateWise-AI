# Contributing
Use public, licensed, or synthetic listing data. Respect source terms, robots rules, rate limits, privacy and intellectual property. Add tests and run `npm run test` plus `npm run build`. Never submit identity documents, agreements, private seller information, credentials, confidential records, or promises of returns.
